# WordPress tools

Each subdirectory is an installable WordPress plugin or integration. Prefer a shortcode or block as the presentation adapter and keep reusable legal rules in `packages/`.

Follow the WordPress coding and security conventions appropriate to the plugin's supported versions. Never commit built ZIP files unless a documented release process requires them.
