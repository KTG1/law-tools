# Standalone applications

Each subdirectory is a deployable legal tool that can run on its own domain. Include deployment configuration, environment-variable documentation, analytics/privacy choices, and the required tool documentation described in the root contributing guide.
